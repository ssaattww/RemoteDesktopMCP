# 通常レビュー

## Dispatch

Requested: gpt-6-sol / high; explicit user override. Fresh normal reviewer, no role override, planned requested profile, applied null, final profile hidden. Judgment-heavy/high criticality review, independent workstreams but decomposition forbidden for reviewer continuity. Normal-persistence owner: normal_review.

## Target and coverage

Mode: bounded normal review / design-fix verification. Reviewer: `normal_review`, separate from the design author and implementation owner. Reviewed committed design HEAD: `320ddb6d5b3a1c54fb4d760612bf4cec4464c5bd` on `feat/tailscale-funnel-design-lint`; base: `83a3de32314edfbcd314f18936b083ac5b1dca9a`; range: `83a3de3..320ddb6`. The current worktree contains concurrent implementation edits, so this pass uses the committed design diff and the three design files at the reviewed HEAD. It does not review product code or claim an implementation verdict. `local_execution_available` is evidenced by PowerShell, Node, npm and Git commands on Windows at `C:\Users\donabe\Project\RemoteDesktopMCP`. No matching current-HEAD CI evidence was supplied.

The complete committed diff has ten paths: three design documents, the terminology whitelist, three reports, two task trackers, and the `skills` symlink. The design-fix scope is the three PR #1 findings in `doc/report/2026-09-24-pr1-file-transfer-design-review.md`, plus the authorized single-PC local MVP boundary. The reports and trackers were checked for scope and accuracy. Implementation paths, current working-tree package edits, remote OAuth/Funnel behavior and multi-PC runtime behavior are outside this bounded stage.

| Required criterion | Disposition | Evidence |
| --- | --- | --- |
| Requirements and design conformance | `checked_no_finding` | The three documents distinguish the local single-PC stage from later Google OIDC, CIMD, refresh-token, Funnel and multi-PC requirements. The original finding contracts are aligned across the changed design passages. |
| Correctness and edge cases | `checked_no_finding` | Download chunks read one completed private copy and compare sent bytes with the begin hash; upload no-replace uses an atomic operation; protected search roots are separated at configuration time. Concurrent-change cases are added to the design tests. |
| Scope discipline and unrelated changes | `checked_no_finding` | The ten committed paths support design repair, planning, skill access and evidence. No product-code change is in this reviewed commit. |
| Changed files and direct dependencies | `checked_no_finding` | Reviewed all ten path diffs and the affected transfer, search, session/node and Desktop Commander passages. The `skills` symlink points to `../CodexSkill/skills`; the reviewer skills were read through it. |
| API, data, configuration, workflow and compatibility effects | `checked_no_finding` | Transfer APIs retain seven tools and `transfer_id` node/session binding. The configuration contract now rejects overlapping real search roots and protects direct file/transfer access. The MVP explicitly uses one local `node_id`. |
| Error handling and failure diagnostics | `checked_no_finding` | Design rejects unreadable copies, digest mismatch, unsupported no-replace capability and upload commit races; temporary files are removed on the stated terminal paths. |
| Security and secret handling | `checked_no_finding` | Protected config is excluded from search/direct file APIs while the existing same-OS-user arbitrary-command trust of `process_start` remains explicit. No secret values were read. |
| Tests and validation adequacy | `held` | Design names the same-size/same-mtime change and replacement, upload race, hash failure and protected-root fixtures. The corresponding executable composition fixtures and focused results must be reviewed with implementation. |
| Current-HEAD CI evidence | `unexplored` | No matching run for `320ddb6d5b3a1c54fb4d760612bf4cec4464c5bd` was supplied; another SHA is not a substitute. Local design lint was run. |
| Report, tracking and documentation accuracy | `checked_no_finding` | Reports describe design changes as rules pending implementation; tasks separate T03–T08 from later F01–F03. |
| Regression and maintainability risk | `held` | Platform-specific no-replace behavior and actual Desktop Commander search composition require implementation evidence. |

`document-wording-review` was applied inside this reviewer using `skills/document-wording-review/SKILL.md` and `references/decision-examples.md`. Reader: `normal_review` on the Windows/PowerShell repository above. Baseline and target are the full commit IDs stated above. The read coverage is every changed passage in the three design documents, the three new reports and two task trackers, plus all three new whitelist definitions and affected uses (`Windows`, `POSIX`, `ハードリンク`). The historical PR #1 report supplied definitions and finding context. No changed human-facing prose was excluded; the symlink is not prose. Meaning, identifier preservation, approved usage and readability are each `checked_no_finding`. The new whitelist entries identify an OS, standard and file operation in the sense used by the revised upload contract. Wording result: `pass`; policy conflicts and missing wording inputs: none. This is separate from mechanical lint.

## Findings and validation

No new required design or wording finding was identified in this bounded pass. The carried findings keep their original identities and severity:

| Finding | Design disposition at `320ddb6d5b3a1c54fb4d760612bf4cec4464c5bd` | Implementation closure still required |
| --- | --- | --- |
| RDMCP-DR-001 / High | Addressed in design: `multi-pc-architecture.md` download section fixes a completed private copy, calculates begin size/hash from it, reads all chunks from that copy, verifies sent bytes, and adds change/replace/delete and digest-failure fixtures. | Actual copy and chunk production path, composed concurrent-change fixture, and focused validation. |
| RDMCP-DR-002 / Medium | Addressed in design: upload commit specifies atomic no-replace for `overwrite=false`, gives Windows/POSIX hard-link semantics, rejects unsupported storage and adds a race fixture. | Actual platform/storage capability and no-replace path, composed competing-writer fixture, and focused validation. |
| RDMCP-DR-003 / Medium | Addressed in design: both requirements and architecture reject search roots with a parent/child relation to the protected config directory, prevent post-search filtering, protect direct access, and add overlap fixtures. | Actual configuration and Desktop Commander call boundary, composed search/direct-access fixtures, and focused validation. |

This table is a design disposition, not finding closure. Every implementation matrix row remains pending for production path, composition fixture and focused evidence; the PR #1 findings must not be reported closed until that matrix is complete and reviewed against an immutable implementation HEAD.

Validation on this Windows worktree: `git diff --check 83a3de3 320ddb6` passed; the scoped design terminology command `node scripts/check-markdown-whitelist.mjs --files doc/design/functional-requirements.md doc/design/multi-pc-architecture.md doc/design/tailscale-funnel-architecture.md` exited 0; `npm.cmd run lint:md` exited 0 with 38 files and no issues. The tracked design files were clean and match the committed target. The markdown count includes concurrent untracked reports, so it is a working-tree lint result rather than an immutable whole-tree result. These checks establish syntax/style only; the wording judgment above is separate. No build, product test, push or CI is claimed for this design-only review.

## Verdict and remaining work

`pass_with_held` for the committed **design-fix stage only**. The design addresses RDMCP-DR-001 / High, RDMCP-DR-002 / Medium and RDMCP-DR-003 / Medium, and the selected local MVP boundary is consistent across the changed documents. The held items are owned by the implementation and integration review: production paths, actual composed race/security fixtures, and focused results for all three findings. Matching CI evidence is unexplored. This verdict does not transfer to the concurrent implementation, a later HEAD, or the PR as a whole.

Next action: review the complete immutable implementation commit and its validation, then verify each carried finding through a complete action/path/fixture/evidence matrix. No implementation fix, commit, push or merge was performed by this reviewer. `reserved_report_paths`: none for independent final attestation in this stage. `report_attestation_allowed`: false; this is a normal review report, not an independent-final-review attestation.

## Complete implementation normal review

This second pass is an initial normal review of the complete immutable implementation HEAD `4eee364a4a72175f6197473ace2e55737115a098`, base `320ddb6d5b3a1c54fb4d760612bf4cec4464c5bd`, with full task context from `83a3de32314edfbcd314f18936b083ac5b1dca9a`. Reviewer continuity is `normal_review`; this reviewer did not author product changes or fixes. The implementation diff contains `.env.example`, the CI workflow, README, package and lockfile, `src/index.ts`, `test/mvp.test.ts`, task status, and five reports. I read all changed paths, the selected-MVP design and verification plan, and the directly used Desktop Commander 0.2.51 compiled search, process and filesystem implementations. The repository was clean at the frozen HEAD before this review-report edit. The local executor is Windows/PowerShell, Node 24.20.0, npm and Git; commit is `committed`, push and matching CI state are not evidenced here. The private test fixtures below used `%TEMP%` and did not edit product source.

### Required findings

**RDMCP-DR-003 — Medium — carried finding remains open; protected config reaches search and transfer.** Origin: PR #1 finding, implementation verification at `src/index.ts:158-162,172-173,179-180`; related audit path at `src/index.ts:84`. The design requires rejecting any search range with an alias to the config and excludes that file from file and transfer APIs. `search()` sends the allowed root straight to Desktop Commander's `start_search` without screening hard-link aliases. In a live isolated Desktop Commander fixture, a hard link from the child config into the allowed root was rejected by `file_read` but `content_search` returned the config's content and path. A separate live fixture started an upload, replaced its closed `.upload` temp pathname with a hard link to the config, then called `file_transfer_upload_chunk`; the tool returned success and the config grew by one byte. The upload write uses `writeFile(..., {flag:"a"})` on the pathname after the begin handle was closed, so it writes through the replacement inode. The ready audit event also records `configPath` despite the design's audit exclusion. Impact: a protected file is exposed through search and can be modified by a transfer API; the protected path is present in audit. Required action: reject aliases before search or enforce an equivalent non-traversing search boundary, hold and verify an upload temp file identity/handle through writes and commit, avoid logging the config path, and add composed hard-link/search and temp-swap fixtures. The deliberate same-OS-user arbitrary-command trust model is unchanged; this finding concerns the promised file/transfer API boundary. No severity reclassification is made.

**RDMCP-NR-001 — High — required Node 22 test gate fails.** Origin: introduced by implementation/test integration; location `.github/workflows/lint.yml` Windows/Ubuntu jobs, `package.json` test script, `test/mvp.test.ts:40-60`. Both CI jobs select Node 22. Independent Node 22.23.3 execution of the frozen HEAD's MVP test exited 1: test 1 passed, OAuth/MCP test 2 was `cancelledByParent` with `ERR_TEST_FAILURE: Promise resolution is still pending but the event loop has already resolved`, and test 3 was then cancelled. Evidence is `%TEMP%\runtime-check-21e762a9abac455fa0a770eb29dac12a\mvp.stdout.log`; Node 24 `npm.cmd test` passes all three. The exact unresolved promise remains to be isolated; it must not be described as a confirmed handler cause. Impact: the required CI runtime cannot validate this commit, so its acceptance gate fails. Required action: resolve the Node 22 lifecycle/test issue and rerun the actual Node 22 test plus the configured CI jobs on the fix HEAD.

**RDMCP-NR-002 — Medium — expired transfers can be revived and temporary files persist.** Origin: introduced by implementation; location `src/index.ts:128-133,154-156,177-183`. Expiry only runs when a session is used or listed, with no timer/sweep; it queues cleanup asynchronously under the transfer lock. `transfer()` calls `session()`, then updates `item.touched` and returns the item before the queued expiry check executes. A focused frozen-build reproduction set an active transfer's `touched` to 31 minutes ago, called `transfer()`, and observed `returned:"tid", state:"active", touchedFresh:true` after the queued check. A session that expires is removed without cancelling its transfers. Download-copy and upload-commit pre-`try` errors can also leave untracked or active temp artifacts, while terminal entries remain in the map. Impact: the 30-minute expiry and cleanup contract does not hold; stale transfers can continue and files can remain indefinitely. Required action: expire and clean under the same lock before touching/using a transfer, sweep idle and orphaned artifacts independently of later requests, clean on session expiry and all failed begin/commit paths, and test timeout, session expiry, cancel, failure and restart cleanup.

**RDMCP-NR-003 — Medium — file and content search can return incomplete results as final output.** Origin: introduced by implementation; location `src/index.ts:158-162,172-173`. Desktop Commander's `start_search` launches ripgrep asynchronously, and `get_more_search_results` can report `IN PROGRESS` and `More results available`. The wrapper calls it once at offset 0 and immediately calls `stop_search`, while returning only that first output; the external caller receives no usable continuation ID. The live config-alias fixture produced exactly this `IN PROGRESS` / `More results available` response before the wrapper stopped the session. A larger or slower tree therefore loses later matches. Also, `content_search` promises string search but omits Desktop Commander's `literalSearch: true`, so regex metacharacters change meaning or can fail. Impact: ordinary filename/content search can miss permitted files and cannot reliably search literal strings. Required action: wait/page until completion or expose a safe continuation protocol, preserve bounded result limits, choose literal content semantics, and test delayed/multi-page and metacharacter cases.

**RDMCP-NR-004 — Medium — process output and termination audit are incomplete.** Origin: introduced by implementation; location `src/index.ts:184-190`. `observe()` requests `read_process_output` with `offset:0,length:1000`. In Desktop Commander 0.2.51 those are line-pagination parameters: the response can contain `remaining` lines, and a completed process's `offset:0` always reads its first 1000 retained lines. The wrapper parses an exit marker, sets the logical process to `finished`, and thereafter returns cached output without retrieving the remainder. Thus output beyond the first page is inaccessible through `process_output`. `process_kill` sets `finished` without recording an observed exit code/result; neither natural completion nor kill emits the required `process.exit` audit event. The short marker test does not cover pagination or kill. Impact: users lose output and the audit trail misses the required termination result. Required action: page DC output to the requested/available end with a stable cursor, preserve completion state and exit code, record termination audit for both paths, and exercise >1000 lines and kill in live-DC tests.

**RDMCP-NR-005 — Medium — local OAuth accepts malformed PKCE and unvalidated authorization parameters.** Origin: introduced by implementation; location `src/index.ts:208-239`. `/authorize` accepts any nonempty `code_challenge` and ignores a requested `scope`; `/token` accepts any nonempty `code_verifier` and does not compare a supplied `redirect_uri` with the authorization request. The current live test uses `a-verifier`, a short low-entropy value, confirming this weak path is part of the accepted fixture. [RFC 7636](https://www.rfc-editor.org/rfc/rfc7636.html) defines 43–128 unreserved characters for verifier and challenge; [RFC 6749](https://www.rfc-editor.org/rfc/rfc6749.html) requires the token redirect URI to equal the one in the authorization request and defines `invalid_scope`. Impact: the advertised PKCE S256 authorization-code contract and the verification plan's invalid-scope/redirect cases are not enforced. Required action: validate PKCE challenge/verifier syntax and length, require the registered exact redirect URI at the token exchange, reject unsupported scope at authorization, and add negative HTTP tests. This concerns the local password OAuth flow; Google OIDC and public-client policy remain in P4.

**RDMCP-NR-006 — Low — session list omits specified expiry and state.** Origin: introduced by implementation; location `src/index.ts:17,168-170`. Functional requirements say a session retains creation, last-use, expiry and state, and `session_list` returns expiry. The model stores only `created`/`touched`, and the list returns only `session_id`, `created_at` and `last_used_at`. Impact: clients cannot tell when a session will expire from the documented API. Required action: expose the effective expiry (and store/derive the required state) and test it across activity and expiration.

### Carried finding completeness matrix

| Finding / required action | Production path | Actual composition fixture | Focused evidence | Disposition |
| --- | --- | --- | --- | --- |
| DR-001 / fix begin hash to sent bytes through one copy | `src/index.ts:157,177-178` creates/copies, hashes, streams and verifies the fixed snapshot | `test/mvp.test.ts:63-75` changes source bytes at same size/mtime; no same-name replacement/deletion, snapshot read failure or multi-chunk digest-failure fixture | Node 24 test pass; Node 22 suite cancellation | `partial`, not closed; original High preserved |
| DR-002 / atomic `overwrite=false` and race cleanup | `src/index.ts:179-181` uses same-directory temp and `link()`; existing competing destination survives | `test/mvp.test.ts:75-81` creates racing target and asserts content; does not assert temp cleanup or unsupported hard-link storage/start-time capability | Node 24 test pass; Node 22 suite cancellation | `partial`, not closed; original Medium preserved |
| DR-003 / one protected search/config contract, direct access and aliases | `src/index.ts:108-116,136-153` rejects overlapping root and direct config inode; `src/index.ts:158-162,179-180` lack alias/temp protection | `test/mvp.test.ts:34-38` covers one overlap; live reviewer fixtures demonstrate config content leak and modification | Focused live reviewer probes fail the required protection | `open defect`, original Medium preserved |

No finding is reclassified. A completeness row marked `partial` cannot be used as closure evidence; the missing cells require implementation and fixed-HEAD validation before bounded fix verification.

### Coverage, wording and validation

| Required criterion | Disposition | Evidence |
| --- | --- | --- |
| Requirement/design conformance | `checked_finding` | DR-003 and NR-002 through NR-006; P4 Google/Funnel/multi-PC deferment is correctly maintained. |
| Correctness and edge cases | `checked_finding` | Live config/search/upload bypass, transfer resurrection, async search and process pagination. |
| Scope discipline | `checked_no_finding` | Implementation and dependency/CI changes serve the single-PC local MVP; no public Funnel or multi-node implementation was claimed. |
| Changed files/direct dependencies | `checked_finding` | Full 14-path implementation diff plus Desktop Commander search/terminal/filesystem implementation and SDK transport use inspected. |
| API/data/config/workflow/compatibility | `checked_finding` | OAuth contract, session list, search, transfer and Node 22 CI defects. |
| Error handling/diagnostics | `checked_finding` | Transfer expiry/failure cleanup and unresolved Node 22 test failure. |
| Security/secret handling | `checked_finding` | DR-003 config content/modify bypass and logged protected path; arbitrary-command trust is intentional. |
| Tests/validation adequacy | `checked_finding` | Three grouped tests omit most verification-plan negative cases, ordinary file/kill coverage and finding-completeness fixtures. |
| Current-HEAD CI | `unexplored` | No matching GitHub Actions run for `4eee364a4a72175f6197473ace2e55737115a098` supplied. Independently reproduced local Node 22 failure is a required gate finding, not a CI-success claim. |
| Reports/tracking/documentation | `checked_finding` | Implementation report's broad protected-config/transfer claims exceed observed behavior; task T06 remains boundary-checking. |
| Regression/maintainability | `checked_finding` | Async DC parsing, unbounded terminal transfer map, OS/file identity races and Node-version divergence. |

`document-wording-review` was reapplied by `normal_review` on the complete before/after prose of README, `.env.example`, workflow labels, the new verification and implementation reports, updated environment/work-context/task reports, and this report's baseline context. The Skill and decision examples read paths are the same as in the design-stage record. Target identity is `320ddb6d5b3a1c54fb4d760612bf4cec4464c5bd..4eee364a4a72175f6197473ace2e55737115a098`; no new whitelist approval was changed. Meaning is `checked_finding`: `reports/2026-09-25-implementation.md` describes the upload temporary file as private and broadly claims protection against config-file hard links, while live transfer/search paths disprove that breadth. Identifier preservation, approved usage and readability are `checked_no_finding`; no policy conflict or wording input is missing. Wording result: `fail` pending accurate limitation/evidence wording. This judgment is separate from mechanical Markdown/term lint.

Independent review execution on the frozen Windows HEAD: `npm.cmd test` under Node 24 exited 0 with 3/3 tests; `git diff --check 320ddb6 4eee364` exited 0; a separate Node 22.23.3 run exited 1 with two tests cancelled. The author reports `npm.cmd run check`, `build`, `lint` and production-only audit passing; those are author evidence, not new reviewer executions. A full dependency audit after overrides is reported by the environment owner as zero vulnerabilities. Matching GitHub CI, push and independent-final-review evidence remain unavailable at this point.

**Implementation verdict: `fail` on `4eee364a4a72175f6197473ace2e55737115a098`.** Required findings are carried DR-003 / Medium and NR-001 / High, NR-002 through NR-005 / Medium, NR-006 / Low. DR-001 / High and DR-002 / Medium remain unclosed because their action/path/fixture/evidence rows are incomplete; this does not lower their original severity. The next action is implementation repair and focused complete fixtures, then a new immutable HEAD for this same normal reviewer to perform bounded fix verification. No source fix, commit, push or merge was made by this reviewer. `report_attestation_allowed: false` for this normal-review stage.

## Fix verification on 23bd136

Mode: same normal reviewer's finding-limited fix verification, with direct-impact and newly changed-area inspection. Reviewed implementation HEAD: `23bd1363d2b027c7d1233f41e8c8ee9ae0120423`; previous reviewed HEAD: `4eee364a4a72175f6197473ace2e55737115a098`; base for the fix diff: `4eee364a4a72175f6197473ace2e55737115a098`. Reviewer `normal_review` did not implement fixes. This pass inspected the full 14-path fix diff, complete `src/index.ts` transfer/search/session/process/OAuth paths, new and changed tests, package/env changes, and all changed reports/tasks. The local single-PC scope and deliberate same-OS-user command trust remain the accepted boundary. Google OIDC, CIMD, refresh tokens, Funnel publication and multi-PC routing are F01–F03 work, outside this verdict.

### Finding closure matrix

| Finding / source severity | Required production action | Actual composition fixture and focused evidence | Disposition |
| --- | --- | --- | --- |
| RDMCP-DR-001 / High | `src/index.ts:217,274-275` copies once into a private snapshot, hashes it, streams sequential bytes, compares the accumulated digest and terminalizes failed reads. | `test/regressions.test.ts:25-56` replaces the source with same-size/same-mtime content, reads three chunks, compares aggregate bytes/hash and removes a snapshot during transfer. Node 22 test 4 passed. | `closed` for the original finding; no severity change. |
| RDMCP-DR-002 / Medium | `src/index.ts:218-230,276-278` probes the destination's atomic `linkNoReplace` capability before begin, uses an exclusive pinned temp and `link` for no-replace commit, and cleans a losing temp. | `test/regressions.test.ts:59-84` runs a real competing destination and injected `ENOTSUP` capability seam; both preserve/clean the expected files. Node 22 tests 5–6 passed. The injected unsupported case is a narrow capability test, not a claim that the host disk lacks hard-link support. | `closed` for the original finding; no severity change. |
| RDMCP-DR-003 / Medium | `src/index.ts:131-143,172-205,232-234,276-278` rejects overlapping real roots, preflights search aliases, rejects direct config inode aliases, pins upload temp identity and omits config path from ready audit. | `test/regressions.test.ts:87-108,232-238` exercises direct hard-link read, content search preflight, temp-path swap and resolved root overlap with live DC. Node 22 tests 7 and 12 passed. | `closed` for the original finding; no severity change. |
| RDMCP-NR-001 / High | `src/index.ts:88-106,365` bounds/references DC shutdown and closes HTTP transport after response finish or abort. | Existing HTTP test and eleven live fixture teardowns run under actual Node 22.23.3. Complete 12-test suite exited 0 without cancellation. | `closed`; no severity change. |
| RDMCP-NR-002 / Medium | `src/index.ts:139-148,158-169,206-216,275-294` serializes expiry before transfer use, runs a minute sweep, expires session-owned transfers, caps terminal records and removes recognized restart artifacts. | `test/regressions.test.ts:110-152` ages transfers/sessions, checks terminal state and file cleanup, cancel, and restart sweep. Node 22 tests 8–9 passed. The broad filename deletion in this new sweep is a separate finding NR-008 below. | `closed` for stale-transfer revival and ordinary lifecycle cleanup; no severity change. |
| RDMCP-NR-003 / Medium | `src/index.ts:232-259` uses literal content search and tries to page DC results. | `test/regressions.test.ts:154-163` uses 115 matches and `[term]`, but checks one incidental filename and total count. It does not prove all 115 distinct results are returned. | `open`: see direct code evidence below. |
| RDMCP-NR-004 / Medium | `src/index.ts:295-314` pages process output and records exit when `observe()` sees it. | `test/regressions.test.ts:165-188` verifies >1000 lines and polled natural/kill exit paths on Windows Node 22. | `open`: unobserved natural exits still lack audit; see below. |
| RDMCP-NR-005 / Medium | `src/index.ts:332-363` validates 43-character S256 challenge, 43–128-character verifier, exact token redirect URI, supported scope and issuer claim. | `test/regressions.test.ts:191-229` uses real HTTP/MCP, including bad PKCE/scope/redirect, replay, claims and file operations. Node 22 test 11 passed. | `closed`; no severity change. |
| RDMCP-NR-006 / Low | `src/index.ts:17,265-266` stores expiry/state and exposes them in session responses. | `test/regressions.test.ts:110-137` checks active row metadata and internal expired state, but never lists after close/expiry. | `open`: active-only list contract remains violated; see below. |

### Remaining required findings

**RDMCP-NR-003 — Medium — incomplete search pagination remains.** Origin: carried implementation finding, still present at `src/index.ts:243-259`. The loop reads `get_more_search_results` with `length:100`, parses a possible next offset, then breaks on `Status: COMPLETED` even when that response says `More results available`. For a completed 115-result search, the initial DC text contains at most ten results and page zero contains results 0–99; results 100–114 are never fetched. The 115-result fixture can pass because it asserts only a filename that may be among the initial ten and a metadata total of 115. The five-page/deadline bound can likewise return `IN PROGRESS` as a successful final result without a usable continuation. Impact: callers receive an incomplete answer. Required action: continue through all advertised pages until complete with no more results, and make a bounded incomplete search explicit rather than silently successful. Assert all 115 distinct results, especially indices 100–114, in the live fixture.

**RDMCP-NR-004 — Medium — unobserved process completion has no audit event.** Origin: carried implementation finding, still present at `src/index.ts:295-314`. The output paging and observed kill path are fixed, but the only `process.exit` audit call is inside `observe()`, which runs solely during `process_output`, `process_status`, or `process_kill`. A process started and allowed to finish without a later tool call never emits its required exit-result audit. The regression fixture explicitly polls until finished, so it cannot catch this. Impact: the audit trail loses natural completions whenever a client does not poll. Required action: independently observe owned process completion while the service is running, record the actual exit result once, and test a short process that finishes without follow-up process calls.

**RDMCP-NR-006 — Low — `session_list` includes inactive sessions.** Origin: carried implementation finding, still present at `src/index.ts:158-169,265-267`. The requirement says the list contains active sessions only. The new list filters solely by user after the sweep; expired and closed sessions remain in the map for up to another session TTL, so both appear in `session_list`. Impact: clients see ended sessions as available and must infer validity despite the advertised active-only list. Required action: filter by `state === "active"` and effective expiry, and assert absence after close and expiry in a live fixture.

**RDMCP-NR-007 — High — new regression test cannot run as written in Ubuntu CI.** Origin: introduced by the fix commit at `test/regressions.test.ts:165,175`; direct workflow effect in `.github/workflows/lint.yml` Ubuntu Node 22 `npm test` job. The test unconditionally starts `for /L %i in (1,1,1005) do @echo line-%i` and `ping -n 3 127.0.0.1 > nul`, which are Windows `cmd.exe` commands. Desktop Commander selects `/bin/sh` on Ubuntu; those commands cannot produce the required 1,005 lines or Windows ping behavior there. Windows Node 22 pass does not validate the Ubuntu job. Impact: the configured pull-request CI test gate fails on Ubuntu. Required action: make process fixture commands platform-specific while preserving the same output and kill assertions, then run the Ubuntu Node 22 job or an equivalent Linux Node 22 test on the fix HEAD.

**RDMCP-NR-008 — Medium — startup cleanup can delete unrelated files in allowed roots.** Origin: introduced by fix at `src/index.ts:141-143`. `removeOrphans` recursively visits every configured file root and deletes any regular file matching `/^\.__rdmcp_[A-Za-z0-9_-]+\.upload$/` without checking a persisted transfer record or ownership. An ordinary file named `.__rdmcp_notes.upload` matches and is removed at service startup. The restart fixture deliberately creates a matching file and labels it owned, but no source evidence distinguishes it from unrelated user data. Impact: starting the server can remove user files inside allowed roots. Required action: persist enough service-owned transfer identity to identify abandoned temps (or use an equivalent exclusive ownership proof) before deletion, and add a fixture proving that an unrelated matching file survives while a known orphan is cleaned.

### Evidence and verdict

Independent local validation on unchanged implementation HEAD `23bd1363d2b027c7d1233f41e8c8ee9ae0120423`: `npx.cmd --yes --package=node@22.23.3 node node_modules/tsx/dist/cli.mjs --test test/**/*.test.ts` exited 0, 12 passed, 0 failed/cancelled/skipped, 51.390 seconds. `npm.cmd run lint`, `npm.cmd run check`, `npm.cmd run build`, and `git diff --check 4eee364 23bd136` exited 0. Node 22 verification capability is local and direct; commit is present, push and matching GitHub CI are not evidenced here. The Ubuntu issue is source-evidenced, pending Linux execution.

Required coverage: requirement/design conformance `checked_finding` (NR-003/004/006); correctness and edge cases `checked_finding` (NR-003/008); scope discipline `checked_no_finding`; changed files/direct dependencies `checked_finding` (all 14 changed paths and directly used DC search/process contracts inspected); API/data/config/workflow compatibility `checked_finding` (NR-007); error handling `checked_finding` (NR-003/008); security/secret handling `checked_no_finding` for the repaired DR-003 path within the accepted same-OS-user trust boundary; tests/validation `checked_finding` (fixture omissions and Ubuntu test); current-HEAD CI `unexplored` (no matching run supplied); reports/tracking/documentation `checked_finding` (the fix matrix says NR-003/004/006 passed without their remaining contract cases, and restart cleanup is called owned without proof); regression/maintainability `checked_finding` (broad cleanup and incomplete DC pagination).

`document-wording-review` was reapplied by the same `normal_review` reader to every changed human-facing passage in `.env.example`, package script labels, `reports/2026-09-25-implementation.md`, `reports/2026-09-25-review-fixes.md`, `reports/2026-09-25-regressions.md`, updated work-context/tasks, and the prior normal-review report. Baseline is `4eee364a4a72175f6197473ace2e55737115a098`, target is `23bd1363d2b027c7d1233f41e8c8ee9ae0120423`; the previously read Skill and decision examples still govern. Meaning `checked_finding`: the new reports describe the broad startup glob as only owned cleanup and claim NR-003/004/006 completion despite missing behavior above. Identification, approved usage and readability `checked_no_finding`; no policy conflict or missing wording input. Wording result: `fail` pending precise report wording. Mechanical Markdown/term lint passed separately.

**Fix-verification verdict: `fail` on `23bd1363d2b027c7d1233f41e8c8ee9ae0120423`.** Original DR-001 / High, DR-002 / Medium and DR-003 / Medium are now closed with complete production/fixture/focused evidence; NR-001 / High, NR-002 / Medium and NR-005 / Medium are also closed. Required items are carried NR-003 / Medium, NR-004 / Medium, NR-006 / Low, plus new NR-007 / High and NR-008 / Medium. All severities and IDs are preserved; no reclassification is made. The next review should be bounded to these five fixes, their direct impact and changed areas on a new immutable HEAD. No fix, commit, push or merge was performed by this reviewer. `report_attestation_allowed: false` for this normal-review stage.

## Bounded fix verification on e66229e

Mode: same normal reviewer's finding-limited verification. Immutable target `e66229ed89027b2aa283e489761c339f122eccfd`, previous target `23bd1363d2b027c7d1233f41e8c8ee9ae0120423`. I inspected the complete ten-path fix diff, affected `src/index.ts` search/session/process/transfer-cleanup paths, their live fixtures and direct Desktop Commander call behavior, plus all changed prose and tracking. This section evaluates the committed candidate, not subsequent working-tree edits. The accepted product remains a local single-PC MVP with same-OS-user command trust. Google OIDC, CIMD, refresh tokens, Funnel, and multi-PC routing remain P4.

### Finding and evidence matrix

| Finding / retained severity | Production action and fixture | Evidence and disposition on e66229e |
| --- | --- | --- |
| RDMCP-DR-001 / High | The unchanged private snapshot/hash/chunk path and multi-chunk replacement/deletion fixture remain in `src/index.ts` and `test/regressions.test.ts`. | `closed` as previously verified; no changed path reopens its original snapshot finding. |
| RDMCP-DR-002 / Medium | The unchanged no-replace `link` commit, capability probe and race/unsupported-storage fixtures remain. | `closed` as previously verified; no changed path reopens its original no-replace finding. |
| RDMCP-DR-003 / Medium | `safePath()` and search preflight still compare aliases with the current Desktop Commander config inode; the live fixture repeats a hard-link read after an upload-temp path swap. | `reopened` by the matching Ubuntu Node 22 CI run: the second alias `file_read` unexpectedly succeeded at `test/regressions.test.ts:110`. The initial alias read/search and swapped-temp write rejection passed. The current-inode-only comparison at `src/index.ts:217-246` does not explain the changed result by itself; whether Desktop Commander replaced its config inode during the fixture needs direct identity tracing. Original Medium severity and ID are retained. |
| RDMCP-NR-001 / High | Shutdown/transport lifecycle is unchanged. | `closed` from prior complete Node 22 evidence; this candidate's local focused tests had no cancellation. |
| RDMCP-NR-002 / Medium | Transfer expiry and cleanup remain; startup cleanup now consults the owned-upload manifest. | `closed` for its original lifecycle finding. The focused Node 22 expiry fixture passed. |
| RDMCP-NR-003 / Medium | `search()` follows an advertised offset after `COMPLETED`, bounds collection and labels incomplete output (`src/index.ts:277-310`). The fixture asserts the exact set of all 115 distinct filename and content matches. | `closed` for pagination: focused local Node 22 fixture passed, including indices 100–114. Ubuntu's corresponding portable-process/search test passed in the matching CI run; Windows CI's separate DC search rejection is recorded as NR-009 below. |
| RDMCP-NR-004 / Medium | A non-overlapping watcher observes each live DC process without a client status call, parses numeric and unknown exit codes, and audits once; `process_kill` uses session-scoped `force_terminate` with a two-second SDK timeout (`src/index.ts:345-410`). The fixture asserts an unpolled natural exit, 1,005 output lines, a live ready child, bounded kill and an event/id match on the same JSONL row. | `closed` for the original missing natural-exit audit and observed kill path. The focused local Node 22 fixture and the corresponding Ubuntu CI test passed. A timed-out termination is explicitly reported as unconfirmed; no actual-exit assertion is made for that branch. |
| RDMCP-NR-005 / Medium | OAuth implementation/negative fixture unchanged. | `closed` from prior complete HTTP evidence. The matching Windows CI's later authenticated file read is a separate DC integration failure, not evidence that token validation regressed. |
| RDMCP-NR-006 / Low | `session_list` now filters `state === "active"` after a sweep (`src/index.ts:317`); fixture asserts closed and expired IDs are absent. | `closed`; focused local Node 22 expiry fixture passed. |
| RDMCP-NR-007 / High | Windows shell loops and ping were replaced with portable Node scripts in `test/regressions.test.ts:184-213`; the live ready child is checked as running before kill. | `closed` for the Windows-only fixture defect: the portable process case executed and passed in the matching Ubuntu Node 22 CI job. This does not imply the whole Ubuntu job passed. |
| RDMCP-NR-008 / Medium | Startup consults `DATA_DIR/transfers/owned-uploads.json`, checks the declared root and `dev`/`ino` before removing an upload temp (`src/index.ts:175-215`), and no longer sweeps lookalike names through roots. Fixture checks one manifest-owned orphan is removed and one unowned lookalike survives. | `closed`; focused local Node 22 fixture passed. The manifest is service-private data; this evidence covers ordinary restart cleanup, not adversarial modification by the same trusted OS account. |

### Required new CI finding

**RDMCP-NR-009 — High — allowed-root Desktop Commander file operations fail in matching Windows CI.** Origin: observed on candidate `e66229e`, distinct from the original Ubuntu-only command-fixture defect. The [Windows Node 22 job in run 36034757381](https://github.com/ssaattww/RemoteDesktopMCP/actions/runs/36034757381) failed the 115-result `file_search` at `test/regressions.test.ts:173` with `Desktop Commander rejected the operation`, and the authenticated HTTP/MCP `file_read` at `test/regressions.test.ts:273` returned `isError`; ten other tests passed, including DR-003. The service reports DC ready after checking its isolated `allowedDirectories` config (`src/index.ts:68-93`), yet these allowed-root operations failed later. The precise shared cause is unconfirmed. Impact: the configured Windows CI gate and ordinary file tools are unreliable on this candidate. Required action: trace the effective DC config and allowed-root resolution at the failing operation, correct the integration or fixture as evidence dictates, retain live file-search and authenticated read assertions, and obtain a passing matching Windows Node 22 job. Investigate alongside Ubuntu DR-003 because config inode/config lifecycle may be common, without treating that hypothesis as proven.

The [Ubuntu Node 22 job in the same run](https://github.com/ssaattww/RemoteDesktopMCP/actions/runs/36034757381) passed 11 tests and failed DR-003's second alias read. This is a current-HEAD CI failure, even though it proves the portable NR-007 process fixture executes on Linux. DR-003's required action is to establish whether the protected config's identity changes during ordinary DC activity, then enforce the intended hard-link protection over the actual service-owned config content and add a Linux regression that checks the identity transitions and final denial. No assumption that the alias still points at the current config inode should replace that evidence.

### Validation, coverage, and wording

Independent reviewer execution on the immutable candidate: actual Node 22.23.3 focused DR-003, NR-002/006, NR-003/004 and NR-008 fixtures exited 0 (4/4, 24.389 seconds); `npm.cmd run check`, `npm.cmd run build`, `npm.cmd run lint`, and `git diff --check 23bd136 e66229e` exited 0. The implementation/regression owners report a complete local Node 22 suite of 12/12, zero failed/cancelled/skipped in 51.944 seconds. Matching CI run 36034757381 supersedes a local-only pass as the acceptance gate: both OS jobs failed as described above. No complete matching green CI result is available.

| Required criterion | Disposition | Evidence |
| --- | --- | --- |
| Requirement/design conformance | `checked_finding` | DR-003 Linux protection failure; NR-003/004/006 local contract fixes verified. |
| Correctness and edge cases | `checked_finding` | 115 distinct hits, unpolled natural audit, active-only session list and manifest-owned cleanup pass locally; cross-platform alias and Windows file operations fail CI. |
| Scope discipline | `checked_no_finding` | Local single-node boundary and root-PID process-tree limitation are explicit; P4 remains deferred. |
| Changed files and direct dependencies | `checked_finding` | Entire ten-path fix diff and affected DC search/process/config integration inspected. |
| API/data/config/workflow compatibility | `checked_finding` | DR-003 and NR-009 CI behavior; NR-007 Linux portability verified despite whole-job failure. |
| Error handling/diagnostics | `checked_finding` | Windows DC rejection lacks a root-cause trace in current evidence; kill timeout is explicitly unconfirmed. |
| Security/secret handling | `checked_finding` | Ubuntu alias read shows DR-003 denial is not stable through the fixture. |
| Tests/validation adequacy | `checked_finding` | New exact-set, active-only, unpolled and owned/unowned fixtures are substantive; matching CI exposes remaining failures. |
| Current-HEAD CI | `checked_finding` | GitHub Actions run 36034757381, Ubuntu 11/12 and Windows 10/12. |
| Reports/tracking/documentation | `checked_no_finding` | Changed reports accurately state local success and Ubuntu CI pending at publication; they do not claim a green CI result. |
| Regression/maintainability | `checked_finding` | Protected config identity and DC allowed-root behavior need stabilized cross-platform evidence. |

`document-wording-review` was reapplied to the complete before/after README, implementation and regression reports, work-context and task status, and this new review passage. Technical meaning, retained identifiers, evidence boundary, approved usage and readability are `checked_no_finding`: README states the root-PID process-tree limit and unconfirmed kill result; implementation reports distinguish local Node 22 success from pending remote Ubuntu execution. The later CI failures change the review verdict, not the truth of those dated local observations. Wording result: `pass` separately from mechanical Markdown/term lint.

**Bounded fix-verification verdict: `fail` on `e66229ed89027b2aa283e489761c339f122eccfd`.** DR-003 / Medium is reopened by matching Ubuntu CI, and new NR-009 / High records matching Windows CI allowed-root failures. DR-001/002 and NR-001/002/003/004/005/006/007/008 are closed for their defined findings, without changing severity or implying whole-job success. Recheck the two required CI failures and their direct effects on the next immutable HEAD. No source fix, commit, push or merge was performed by this reviewer. `report_attestation_allowed: false`.

## Bounded fix verification on 230af93

Mode: same normal reviewer's verification of reopened DR-003 and NR-009 plus their direct effects. Immutable source target `230af93bb4dbde337b3f45a2382edb18fccb53f3`, previous reviewed source `e66229ed89027b2aa283e489761c339f122eccfd`. I inspected the complete nine-path diff, the affected Desktop Commander/config/file guards, all changed tests and human-facing reports/tasks, and Desktop Commander 0.2.51's config mutation source. No in-progress source edit is part of this review. The selected product remains the local single-PC MVP with deliberate same-OS-user arbitrary-command trust; Google OIDC/CIMD/refresh, Funnel and multi-PC routing stay in P4.

### Finding-by-finding disposition

| Finding / retained severity | Production action, actual fixture and evidence | Disposition |
| --- | --- | --- |
| RDMCP-DR-001 / High; DR-002 / Medium | Private snapshot/hash and atomic no-replace production paths and their complete prior fixtures are unchanged by this bounded diff. | `closed` from prior verified evidence; no severity change. |
| RDMCP-DR-003 / Medium | `src/index.ts:197-224,265-295` records protected config `dev:ino` before and after startup/config writes, persists identity history and checks it before reads/searches; `test/regressions.test.ts:103-132` links the old config, atomically replaces it, links the new config, and asserts both aliases and search are refused. Desktop Commander source `config-manager.js:60-66,183-184` confirms onboarding mutation writes a temporary file and renames it over `config.json`. Local actual Node 22 focused fixture passed 1/1, and the authors' full suite passed 13/13. | `open`: matching Ubuntu CI run 36036096436 failed the **first historical-alias read** at `test/regressions.test.ts:116` because it unexpectedly succeeded. The source captures known inodes, but an inode retained as an alias after a later config replacement may not have been observed before the replacement. That timing explanation is a hypothesis, not established by the current diagnostics. The original protection requirement and Medium severity remain. |
| RDMCP-NR-001 / High; NR-002/003/004/005 / Medium; NR-006 / Low; NR-007 / High; NR-008 / Medium | Their previously complete production/fixture rows are unchanged except for the direct config/file path examined here. Ubuntu ran the portable process fixture; local focused and full suites passed without cancellations or skips. | `closed` for their defined findings; no severity or identity change. The Ubuntu file-read failure below is not attributed to OAuth validation. |
| RDMCP-NR-009 / High | `src/index.ts:68-116,153-166` canonicalizes roots for both service and DC, strictly parses `get_config.allowedDirectories` before delegated calls and records a bounded, sanitized DC rejection; `test/regressions.test.ts:135-158` checks delegated read/search through a junction or symlink. Fixtures now live under ignored, validated `reference/validation` paths. Independent actual Node 22 focused fixture passed 1/1 with zero skip; full local suite passed 13/13. The matching Windows Node 22 job passed its complete suite, including ordinary and aliased-root file operations. | `open` for cross-platform allowed-root reliability: matching Ubuntu CI passed 11/13 but its authenticated ordinary `file_read` at `test/regressions.test.ts:319` returned an MCP tool error. Its sanitized audit showed `operation.rejected` and no `desktop_commander.rejected`, so current evidence points to a service-side rejection, without identifying its cause. The Windows-side failure from e662 was repaired or did not recur; that does not close the High integration finding while Linux's ordinary allowed-root read fails. |

### Remaining required action and limits

For DR-003, capture only filesystem identities and protected-history membership at startup and immediately around the Ubuntu historical-alias fixture. Establish whether a Desktop Commander config replacement occurs after the last identity capture, then enforce denial for both prior and current protected config content. A persisted `dev:ino` alone is not a durable ownership proof when the old inode is unlinked and may be reused; keep a private reference to historical content or otherwise prove that reused identities cannot block ordinary files. The Ubuntu alias fixture must reject both old and new aliases on a matching immutable head.

For NR-009, identify the actual public MCP error category at the Ubuntu `file_read` and correlate it with the candidate file's identity and protected-history membership, using safe diagnostics without config contents, path payloads or credentials. Restore ordinary allowed-root read/search on Ubuntu and retain the Windows junction/symlink fixture. These two symptoms may share a config-identity lifecycle cause, but that remains unproven; one corrective change can close both if focused and both-platform CI evidence covers each contract.

Independent local validation on `230af93`: actual Node 22.23.3 focused DR-003 test passed 1/1 in 3.717 seconds; focused NR-009 test passed 1/1 in 9.084 seconds, both with zero failures/cancellations/skips. `npm.cmd run check`, `npm.cmd run build`, `npm.cmd run lint`, and `git diff --check e66229e 230af93` exited 0. Implementation/regression owners' complete local Node 22 suite exited 0 with 13 passed, zero failed/cancelled/skipped/todo, in 57.574 seconds. Matching [GitHub Actions run 36036096436](https://github.com/ssaattww/RemoteDesktopMCP/actions/runs/36036096436) has a successful Windows job and failed Ubuntu job (11 passed, 2 failed). The reported local passes are not substituted for the cross-platform gate.

| Required criterion | Disposition | Evidence |
| --- | --- | --- |
| Requirement/design conformance | `checked_finding` | DR-003 historical alias and NR-009 ordinary allowed-root access still fail Ubuntu. |
| Correctness and edge cases | `checked_finding` | Atomic config replacement fixture fails on Linux; possible unpinned inode reuse requires identity tracing rather than a causal claim. |
| Scope discipline | `checked_no_finding` | Local single-node boundaries remain explicit; no P4 behavior claimed. |
| Changed files/direct dependencies | `checked_finding` | All nine changed paths, DC config-manager mutation, config guards and affected file-tool integration inspected. |
| API/data/config/workflow compatibility | `checked_finding` | Ubuntu historical alias and authenticated read fail; Windows matching job passes. |
| Error handling/diagnostics | `checked_finding` | Sanitized labels avoid contents but do not yet distinguish the Ubuntu service-side rejection reason. |
| Security/secret handling | `checked_finding` | A former config inode can be read through a hard link in Ubuntu CI; DR-003 remains open. |
| Tests/validation adequacy | `checked_finding` | Deterministic old/new-alias and junction fixtures are substantive; Linux CI exposes a remaining mismatch. |
| Current-HEAD CI | `checked_finding` | Matching run 36036096436: Windows success, Ubuntu 11/13 failure. |
| Reports/tracking/documentation | `checked_no_finding` | Changed reports describe local success and pending matching CI without asserting closure. |
| Regression/maintainability | `checked_finding` | Inode-history timing and reuse may affect both protection and ordinary file access; exact cause remains to be proven. |

`document-wording-review` was reapplied to the full before/after regression and repair reports, task statuses, fixture diagnostics and this report, separately from mechanical Markdown lint. Meaning, identifiers, approved usage, readability and evidence boundaries are `checked_no_finding`: the changed prose distinguishes local fixture results from CI acceptance and labels the earlier Windows-cause explanation provisional. Wording result: `pass`.

**Bounded fix-verification verdict: `fail` on `230af93bb4dbde337b3f45a2382edb18fccb53f3`.** DR-003 / Medium and NR-009 / High remain the finite required set. All earlier closed findings retain their identity and severity; no new ID or unproven root-cause claim is added. Recheck these two behaviors and their direct effects on a new immutable head with matching green Windows and Ubuntu Node 22 CI. No source fix, commit, push or merge was performed by this reviewer. `report_attestation_allowed: false`.

## Bounded fix verification on e5823a2

Mode: same normal reviewer's finding-limited verification of DR-003 and NR-009 with direct effects. Immutable target `e5823a2feaa0770e35703027179246c487eb3162`; previous target `230af93bb4dbde337b3f45a2382edb18fccb53f3`. I inspected the full six-path diff, changed config pin/startup/file-tool paths, complete strengthened regression, direct Desktop Commander config/search dependencies, and changed reports/task wording. This is a local single-PC MVP review; same-OS-user arbitrary-command trust and the deferred P4 features are unchanged.

| Finding / retained severity | Production action and actual fixture | Evidence and disposition |
| --- | --- | --- |
| RDMCP-DR-003 / Medium | `src/index.ts:75-84,201-259` seeds onboarding flags, hard-links each observed config inode into a private pin directory, validates persisted pin identity on restart, and prunes only sole-link retired pins. `test/regressions.test.ts:111-161` checks historical/current aliases after atomic replacement and restart, 128 create/delete iterations, ordinary file read, content-search denial and swapped upload-temp rejection. | `open`, despite the DR-003 fixture passing in matching Ubuntu and Windows CI. The pin creation reads `lstat(config)` and then awaits `link(config,pin)` without validating the successfully created pin's inode against the earlier stat. A concurrent atomic DC config replacement between those awaits can pin a new inode under the old identity while leaving an old hard-link alias unrecorded. This is directly in the replacement path under review. Required action: verify the pin identity after successful `link`, handle a mismatch by recording/retrying both actual identities without losing the old reference, and exercise the race or a narrow deterministic seam. Original Medium severity retained. |
| RDMCP-NR-009 / High | Canonical-root and strict `allowedDirectories` checks remain; `tool()` now logs a bounded rejection category. The junction/symlink read/search fixture and authenticated HTTP read/patch/search remain. | `open`: matching Ubuntu Node 22 CI run 36037041391 passed 10/13 and failed three `start_search`-backed operations: NR-009 aliased-root search at `test/regressions.test.ts:170`, NR-003 ordinary 115-hit file search at `:236`, and NR-005 authenticated content search at `:342`. Ordinary HTTP read and patch passed. All three surfaced Desktop Commander rejection, but the present sanitized audit gives only the tool name, not a safe causal detail. The matching Windows job passed. Required action: determine the actual DC `start_search` error category safely, repair the Linux search integration or fixture as evidence dictates, and preserve all three live search assertions in a matching green Ubuntu run. The previous Linux ordinary-read failure did not recur; no unproven shared cause is asserted. |

Previously closed DR-001 / High, DR-002 / Medium, NR-001 / High, NR-002/003/004/005 / Medium, NR-006 / Low, NR-007 / High and NR-008 / Medium retain their finding identities and original closure dispositions. NR-003/005 production contracts are not independently reopened by the Linux search failure; their live search calls provide direct-impact evidence for NR-009. The cross-platform CI gate nevertheless fails on this candidate.

Independent actual Node 22.23.3 focused NR-009 passed 1/1, zero skip (9.203 seconds). Focused DR-003 was inconsistent on the same immutable candidate: one execution failed before alias assertions when Windows returned `EPERM` replacing `config.json` at `test/regressions.test.ts:120` (3.841 seconds), and an immediate repeat passed 1/1, zero skip (7.200 seconds). This is an intermittent fixture/environment failure, not evidence that alias protection failed in that Windows run; make the replacement fixture robust without relaxing its alias assertions. `npm.cmd run check`, `npm.cmd run lint`, and `git diff --check 230af93 e5823a2` exited 0. The authors report a complete local Node 22 suite of 13/13, zero fail/cancel/skip/todo in 62.771 seconds and a passing build. Matching [GitHub Actions run 36037041391](https://github.com/ssaattww/RemoteDesktopMCP/actions/runs/36037041391) passed Windows but failed Ubuntu with the three search cases above.

| Required criterion | Disposition | Evidence |
| --- | --- | --- |
| Requirement/design conformance | `checked_finding` | DR-003 pin race remains in protected-config replacement; NR-009 Linux search integration fails. |
| Correctness and edge cases | `checked_finding` | Pin post-link identity is unchecked; strengthened old/new-alias/restart/churn fixture passes CI but cannot force the race. |
| Scope discipline | `checked_no_finding` | Local MVP and P4 boundary preserved. |
| Changed files/direct dependencies | `checked_finding` | Full six-path diff, config manager atomic rename, DC `start_search`/ripgrep entry path and local guards inspected. |
| API/data/config/workflow compatibility | `checked_finding` | Windows CI succeeds; three Ubuntu search calls fail. |
| Error handling/diagnostics | `checked_finding` | Current DC audit exposes safe tool label but not a causal category for Linux search rejection. |
| Security/secret handling | `checked_finding` | A successful post-`lstat` link can capture a different inode; private pin validation on reload is otherwise present. |
| Tests/validation adequacy | `checked_finding` | Restart/churn assertions are substantive; Windows atomic-replacement fixture intermittently fails with `EPERM`. |
| Current-HEAD CI | `checked_finding` | Run 36037041391, Windows pass, Ubuntu 10/13 with three search failures. |
| Reports/tracking/documentation | `checked_no_finding` | Changed reports accurately describe local evidence and pending CI; they do not claim closure. |
| Regression/maintainability | `checked_finding` | Config pin race and unstable Windows replacement fixture need deterministic handling. |

`document-wording-review` was reapplied to the full before/after repair and regression reports, task status and this passage, separately from Markdown lint. Technical meaning, retained IDs/severity, evidence attribution, approved usage and readability are `checked_no_finding`: the reports do not assert the prior CI root cause and distinguish local success from pending CI. Wording result: `pass`.

**Bounded fix-verification verdict: `fail` on `e5823a2feaa0770e35703027179246c487eb3162`.** DR-003 / Medium and NR-009 / High remain the finite required set for the next immutable head. No source fix, commit, push or merge was performed by this reviewer. `report_attestation_allowed: false`.

## Bounded fix verification on 2775a65

Mode: same normal reviewer's closure pass for DR-003 and NR-009 and their direct effects. Immutable target `2775a6570069b19e0486424ff141d36f60b22067`, previous source `e5823a2feaa0770e35703027179246c487eb3162`. I inspected the full eight-path diff, production config pin and startup paths, the deterministic race fixture, direct DC config/search dependencies, and changed user-facing prose. The local single-PC MVP and same-OS-user arbitrary-command boundary remain unchanged; Google OIDC/CIMD/refresh, Funnel and multi-PC routing remain deferred P4 work.

| Finding / retained severity | Production action and actual composition fixture | Focused/final evidence and disposition |
| --- | --- | --- |
| RDMCP-DR-003 / Medium | `src/index.ts:228-275` serializes config pinning, links the actual config inode under a random private name, obtains identity from the new pin rather than a pre-link stat, persists that verified identity, then checks whether the current config changed and retries while keeping observed history. Only the pre-creation startup call permits a missing config. The private persisted pin is validated on reload; a sole-link retired pin can be pruned without allowing inode reuse to block ordinary files. `test/regressions.test.ts:130-211` first proves a known A alias is protected, checks A/B aliases across atomic replacement, restart and 128 create/delete iterations, and uses the narrow `linkProtectedConfig` hook to retain the exact C inode linked during a B-to-C race. Both known A and captured C are denied while an ordinary file is readable. The Windows replacement retry is bounded to transient sharing errors and leaves all alias assertions intact. | Independent actual Node 22.23.3 focused DR-003 fixtures passed 2/2, zero skip (14.153 seconds). Authors' full local suite passed 14/14, zero fail/cancel/skip. Matching [GitHub Actions run 36039277467](https://github.com/ssaattww/RemoteDesktopMCP/actions/runs/36039277467) passed both Ubuntu and Windows Node 22 jobs, including the deterministic alias/race cases. `closed` for the original protected-config finding; original Medium severity retained. |
| RDMCP-NR-009 / High | Canonical service/DC roots and strict per-call `allowedDirectories` checks remain. The live fixture verifies delegated read/search through a symlink or Windows junction, and the complete suite includes ordinary 115-result filename/content search and authenticated HTTP read/patch/search. Failure-only DC diagnostics now preserve a bounded, independently redacted detail for future triage without emitting config contents or credentials. | Matching run 36039277467 passed both OS jobs with all 14 tests, covering the three Ubuntu `start_search` paths that failed on e582 and the earlier ordinary file paths. Independent check/lint and the authors' build/full local suite passed. `closed` for cross-platform allowed-root/search integration; original High severity retained. The exact cause of the earlier intermittent Ubuntu search rejection was not established and is not claimed as fixed by a particular line. |

Previously closed RDMCP-DR-001 / High, DR-002 / Medium, NR-001 / High, NR-002/003/004/005 / Medium, NR-006 / Low, NR-007 / High and NR-008 / Medium retain their finding identities and closure dispositions. No finding was reclassified. The current matching CI run, not the local fixture result alone, supplies the final cross-platform gate evidence.

Independent reviewer execution on `2775a65`: `npx.cmd --yes --package=node@22.23.3 node node_modules/tsx/dist/cli.mjs --test --test-name-pattern=DR003 test/regressions.test.ts` exited 0, 2/2; `npm.cmd run check`, `npm.cmd run lint`, and `git diff --check e5823a2 2775a65` exited 0. The implementation/regression owners report `npm.cmd run build` passing and the awaited full Node 22.23.3 suite exiting 0 with 14/14 passed, zero failed/cancelled/skipped/todo, in 69.338 seconds. The parent retrieved both matching GitHub Actions jobs by commit: Ubuntu `107767090502` and Windows `107767090233` completed successfully in run 36039277467. No local Linux execution claim is made.

| Required criterion | Disposition | Evidence |
| --- | --- | --- |
| Requirement/design conformance | `checked_no_finding` | Current and successfully pinned historical config identities are protected within the documented same-OS-user boundary; both OS CI jobs pass. |
| Correctness and edge cases | `checked_no_finding` | Link-first verified identity, bounded retry, persisted pin validation and deterministic B-to-C race fixture cover the reopened path. |
| Scope discipline | `checked_no_finding` | No public Funnel, Google OIDC or multi-node behavior was claimed. |
| Changed files/direct dependencies | `checked_no_finding` | All eight changed paths and direct DC config/search dependencies inspected. |
| API/data/config/workflow compatibility | `checked_no_finding` | Canonical allowed-root read/search and authenticated file operations pass matching Windows/Ubuntu CI. |
| Error handling/diagnostics | `checked_no_finding` | Pin change fails closed after bounded attempts; failure-only DC detail is bounded/redacted. |
| Security/secret handling | `checked_no_finding` | A pin record is derived only from a verified linked inode; known historical aliases remain denied. |
| Tests/validation adequacy | `checked_no_finding` | Deterministic race, restart/churn/ordinary-read and both OS full jobs pass without skipped tests. |
| Current-HEAD CI | `checked_no_finding` | Matching GitHub Actions run 36039277467, both jobs successful. |
| Reports/tracking/documentation | `checked_no_finding` | README accurately limits protection to current and captured historical config inodes and states the same-OS unobserved-copy boundary. |
| Regression/maintainability | `checked_no_finding` | Narrow optional pin hook and bounded retries keep the prior race testable without changing public MCP behavior. |

`document-wording-review` was reapplied to the complete before/after README, regression and fix reports, and this report, separately from mechanical Markdown lint. Meaning, identifiers/severity, evidence attribution, approved usage and readability are `checked_no_finding`. The README's limitation is specific to same-OS-user aliases/copies made outside the MCP file API from versions removed before any successful pin; it does not weaken denial for versions actually captured. Reports distinguish author-local evidence from matching CI and do not assign a proven root cause to the earlier NR-009 search failures. Wording result: `pass`.

**Bounded normal-review verdict: `pass` on `2775a6570069b19e0486424ff141d36f60b22067`.** DR-003 / Medium and NR-009 / High are closed with production, fixture, independent focused and matching two-OS CI evidence; all earlier findings remain closed at their original severity. No source fix, commit, push or merge was performed by this reviewer. `report_attestation_allowed: true` for this normal-review stage.

## Independent-finding fix verification on 6a1c5ab

Mode: same normal reviewer's bounded verification of the authoritative `RDMCP-MVP-IFR-001` through `007` findings and their direct effects. Immutable target `6a1c5ab58edab6fff603b418c9f4e5d5e9691a7a`; previous target `eed5623a6b0cc800565a112436adbfce293e0b26`. I inspected the complete eight-path delta, affected process owner/output/termination and protected-config lifecycle paths, changed tests and reports, and relevant design dependencies. I did not perform an unrelated full review or write the reserved independent report. The single-PC local MVP and same-OS-user arbitrary-command boundary remain the accepted scope.

### Finding completion matrix

| Finding / original severity | Production action and actual composition fixture | Evidence and disposition |
| --- | --- | --- |
| RDMCP-MVP-IFR-001 / High | `src/index.ts:456-487` keys the current owner by node, DC generation and PID, stales a predecessor before publishing its successor, and revalidates under the process mutex before delegated read/status/kill and watcher calls. `test/independent-fixes.test.ts:116-160` supplies one PID to successive logical processes and holds a read to exercise both lock orderings, asserting no stale call reaches the successor. | Independent actual Node 22 focused fixture passed; Windows and Ubuntu matching CI passed this test. `closed` for the original PID reuse finding, retaining High severity. A separate output-drain issue under IFR-004 is described below. |
| RDMCP-MVP-IFR-002 / High | Config identity refresh now prunes only sole-link retired pins inside the same mutex before enforcing the 64-pin cap (`src/index.ts:242-280`). `test/independent-fixes.test.ts:36-64` makes 70 controlled atomic replacements against live DC, retains a known hard-link alias, checks ordinary reads and restarts. This is not a claim that DC naturally writes 70 times. | Independent focused fixture and both matching CI jobs passed IFR-002's new test. **Closure held for direct-impact CI evidence:** Ubuntu's existing DR-003 fixture at `test/regressions.test.ts:146` unexpectedly read the just-created replacement alias; the run's diagnostic showed the current config inode and two protected keys, but did not establish the alias inode or precise replacement timing. The README explicitly limits protection to current and successfully pinned history; an outside-API alias to an unobserved transient version may be outside that boundary. Determine which identity the alias held and whether it had been captured before classifying this as a product breach or an overstrong fixture expectation. Restore a deterministic, boundary-consistent assertion and matching green Ubuntu CI before closing this High item. |
| RDMCP-MVP-IFR-003 / Medium | Upload begin applies the same active-transfer cap as download begin before path validation, capability probe or artifact creation (`src/index.ts:440`). `test/independent-fixes.test.ts:65-86` fills the shared cap with ten uploads and ten downloads, then checks overflow rejects without transfer state, temp or manifest change. | Independent focused fixture and both matching CI jobs passed. `closed`; original Medium severity retained. |
| RDMCP-MVP-IFR-004 / Medium | After a `force_terminate` timeout, `process_kill` keeps one watcher and returns `terminating`/`termination_unconfirmed`. `test/independent-fixes.test.ts:162-193` injects delayed completion and asserts one later exit audit without client polling. | The injected timeout fixture passed locally and in both CI jobs, but `watchProcess()` has a directly related undrained-output edge at `src/index.ts:468-472`: `observe()` stops after 100 pages with `completionPending` and remaining lines, then the watcher calls `finishWhenRootIsGone()` whenever its earlier session listing was inactive. That marks the item finished, deletes the PID owner and audits before a later tick can fetch the tail. Required action: carry a drain result across watcher iterations and defer finalization/owner release while buffered pages remain, including an adapter fixture with at least 101 pages and inactive session listing that asserts the last page and one audit. `open`; original Medium severity retained. |
| RDMCP-MVP-IFR-005 / Medium | `process.start` now records caller/session/node/logical ID, PID and a command representation that redacts configured token/hash literals, Bearer values and labeled secret flags (`src/index.ts:461,473`). `test/independent-fixes.test.ts:194-218` checks those bindings and the absence of fixture secrets. | Independent focused fixture and both matching CI jobs passed. `closed`; original Medium severity retained. The audit representation is bounded and remains within private DATA_DIR. |
| RDMCP-MVP-IFR-006 / Medium | The existing upload commit production path is unchanged; `test/independent-fixes.test.ts:87-115` now supplies positive real-MCP multi-chunk evidence for new `overwrite:false` and existing `overwrite:true` destinations, checking exact bytes/hash, terminal state and temp/manifest cleanup. | Independent focused fixture and both matching CI jobs passed. `closed`; original Medium severity retained. |
| RDMCP-MVP-IFR-007 / Low | `doc/design/multi-pc-architecture.md:288` now names pinned Desktop Commander 0.2.51, matching package, lockfile, README and live tests. | Scoped design-term/Markdown lint and both matching CI jobs passed. `closed`; original Low severity retained. |

### Validation and remaining action

This normal reviewer's execution on immutable `6a1c5ab`: actual Node 22.23.3 `test/independent-fixes.test.ts` exited 0 with 6/6 passed, 0 failed/cancelled/skipped/todo, in 38.272 seconds; `npm.cmd run check`, `npm.cmd run lint`, and `git diff --check eed5623 6a1c5ab` exited 0. The authors report a complete local Node 22 suite of 20/20, zero failed/cancelled/skipped, in 67.538 seconds and a passing build. Matching [GitHub Actions run 36042688392](https://github.com/ssaattww/RemoteDesktopMCP/actions/runs/36042688392) passed Windows 20/20 but failed Ubuntu 19/20 solely at the existing DR-003 replacement-alias fixture. These current-HEAD CI results supersede the local-only full pass. The previously closed DR/NR identities and severities remain unchanged; the Ubuntu failure is a direct config-lifecycle gate, not proof by itself that the documented same-OS boundary was violated.

`reports/2026-09-25-independent-fixes.md` contains a stale final-status sentence saying this candidate is `commit_pending` and push/CI are a future stage. It was true when drafted but is inaccurate on committed and pushed `6a1c5ab`; update it to identify local result and the matching CI result separately before final handoff. The other changed prose in the eight-path diff accurately preserves finding IDs, severities and P4 deferral. The reserved `reports/2026-09-25-independent-review.md` remains absent and was not created here.

| Required criterion | Disposition | Evidence |
| --- | --- | --- |
| Requirement/design conformance | `checked_finding` | IFR-004 output-drain ownership edge; IFR-002 direct config-fixture CI gate. |
| Correctness and edge cases | `checked_finding` | 100-page cap can finalize before tail; Ubuntu replacement-alias identity is unresolved. |
| Scope discipline | `checked_no_finding` | No P4/public or multi-PC implementation claimed. |
| Changed files/direct dependencies | `checked_finding` | Entire eight-path diff and affected process/config transfer paths inspected. |
| API/data/config/workflow compatibility | `checked_finding` | Windows 20/20, Ubuntu 19/20 on the exact candidate. |
| Error handling/diagnostics | `checked_finding` | Completion pending must not be audited before drain; alias diagnostic lacks candidate identity. |
| Security/secret handling | `checked_finding` | IFR-005 audit redaction fixture passes; Ubuntu alias read succeeds, but current trace cannot establish whether that inode was in protected history. |
| Tests/validation adequacy | `checked_finding` | Six new focused fixtures pass, but >100-page watcher and deterministic replacement identity cases are missing. |
| Current-HEAD CI | `checked_finding` | Run 36042688392: Windows pass, Ubuntu one existing DR-003 test failure. |
| Reports/tracking/documentation | `checked_finding` | Stale `commit_pending`/push-next status in the independent-fixes report. |
| Regression/maintainability | `checked_finding` | Watcher terminalization and config replacement fixture require targeted regression evidence. |

`document-wording-review` was reapplied to the full changed design sentence, handoff paragraph, new independent-fixes and regression reports, changed task statuses, and this report, separately from mechanical lint. Meaning `checked_finding` for the stale `commit_pending`/push-next sentence; identification, approved usage and readability `checked_no_finding`. No terminology approval changed and no policy conflict was found. Wording result: `fail` until the report states the committed candidate and the latest CI evidence accurately. Mechanical Markdown/term lint passed but does not close the wording finding.

**Bounded normal fix-verification verdict: `fail` on `6a1c5ab58edab6fff603b418c9f4e5d5e9691a7a`.** IFR-001/003/005/006/007 close at their original severities; IFR-002 / High remains held on the direct Ubuntu config-alias CI failure, and IFR-004 / Medium remains open on undrained output finalization. The stale report status also needs correction. Recheck only these fixes and direct effects on the next immutable head, with matching Windows and Ubuntu CI. No product fix, commit, push or merge was performed by this reviewer. `report_attestation_allowed: false`.

## Bounded fix verification on 0696dee

Mode: same normal reviewer's finding-limited verification of IFR-002 / High, IFR-004 / Medium and direct file-identity/config effects. Immutable target `0696dee9261b72875d3797d238464fe6b595f057`, previous target `6a1c5ab58edab6fff603b418c9f4e5d5e9691a7a`. I inspected the full nine-path diff, exact-identity pin/upload cleanup and migration paths, process output-drain logic, changed tests, reports/tasks and README. This is not a new broad review. The local single-PC MVP and same-OS-user trust boundary are unchanged.

| Finding / original severity | Production action and actual composition fixture | Focused/final evidence and disposition |
| --- | --- | --- |
| RDMCP-MVP-IFR-002 / High | `src/index.ts:213-306` now derives config/upload file identities from bigint stats as canonical decimal strings, validates pinned history exactly, and prunes safe sole-link history before the cap. It migrates a legacy config record only if its safe numeric identity exactly matches the retained private pin; unsafe records fail closed. `test/independent-fixes.test.ts:36-64` exercises 70 controlled live replacements, known alias, ordinary read and restart. `test/regressions.test.ts:149-331` now draws A/B aliases from successful private pins, adds adjacent identities above `2^53`, exact manifest and unsafe legacy checks, plus an ordinary read. | `open`: independent actual Node 22 focused DR-003 tests passed 4/4; author local full suite passed 23/23. Matching Ubuntu CI run 36045163545 failed the live-history fixture at restart (`test/independent-fixes.test.ts:59`): `initialize()` exhausted four immediate pin attempts with `Protected config identity changed while pinning` (`src/index.ts:293`). This is a direct ordinary-DC/config-churn failure of IFR-002's runtime and restart contract. Required action: let normal bounded config replacement settle or retry with bounded delay while retaining fail-closed behavior under sustained churn, then prove the 70-version restart on matching Ubuntu CI. The separate DR-003 fixture also failed as described below; its exact cause is not assumed to be the same. Original High severity retained. |
| RDMCP-MVP-IFR-004 / Medium | `src/index.ts:502-503` carries `outputDrained` from each bounded 100-page pass and only finalizes an inactive process after the remaining output is drained. `test/independent-fixes.test.ts:194-237` holds page 101 while verifying current owner, unfinished state and absent exit audit, then releases the final tail/marker and checks one audited completion. | Independent actual Node 22 focused timeout and 101-page cases passed 2/2; both cases also passed the matching Ubuntu and Windows jobs. `closed` for the original timeout and tail-drain finding, retaining Medium severity. |
| RDMCP-MVP-IFR-001 / High; IFR-003/005/006 / Medium; IFR-007 / Low | Their previously verified process owner, transfer cap, audit redaction, positive upload and version-identification production/fixture rows are unchanged except for the exact file-identity direct effects reviewed here. | `closed` from the prior bounded review; original IDs and severities retained. The new exact-identity format does not alter their public contracts. |

The matching Ubuntu job's other failure was the existing DR-003 swapped-upload-temp digest assertion at `test/regressions.test.ts:215`. That assertion hashes the mutable `config.json` pathname before and after an intervening Desktop Commander call; Desktop Commander's own usage-tracker config rewrite may legitimately replace that pathname while the pinned inode is unchanged. It does not, by itself, prove the upload wrote protected content. Required action: compare the exact retained protected inode/alias before and after the attempted upload, keep the rejected write and failed-transfer assertions, and use bigint stats in the A/B alias-to-pin identity assertions so the fixture's claimed precision matches the new production contract. Keep the documented same-OS-user unobserved-version boundary; do not weaken denial for known pinned versions.

The README's new recovery paragraph overstates one branch: it says safely unmatched legacy numeric manifests stop startup. `cleanupOwnedUploadArtifacts()` rejects invalid or unsafe numeric records, but a safe numeric upload record whose existing path has a different inode is preserved as a file while the manifest is rewritten without that record and startup continues. State these branches separately, or change the implementation if preserving that unmatched record is intended. This is a wording/behavior consistency finding, not evidence of user-file deletion. The stale `commit_pending`/push-next sentence from the prior independent-fixes report was corrected on this target; the current report separates `6a1c5ab` CI from this candidate's local results.

This normal reviewer's execution on immutable `0696dee`: actual Node 22.23.3 focused DR-003 tests exited 0 with 4/4 passed, 0 failed/cancelled/skipped/todo, in 25.829 seconds; focused IFR-004 cases exited 0 with 2/2 in 14.608 seconds; `npm.cmd run check`, `npm.cmd run lint`, and `git diff --check 6a1c5ab 0696dee` exited 0. The authors report the awaited full local Node 22 suite passed 23/23, zero failed/cancelled/skipped, in 73.234 seconds, with build passing. Matching [GitHub Actions run 36045163545](https://github.com/ssaattww/RemoteDesktopMCP/actions/runs/36045163545) has an Ubuntu 21/23 failure in the two config cases above and a successful Windows job with 23/23 passed. Current-HEAD CI therefore does not pass, regardless of local results.

| Required criterion | Disposition | Evidence |
| --- | --- | --- |
| Requirement/design conformance | `checked_finding` | IFR-002 live config-history restart fails Ubuntu; IFR-004 drain behavior is repaired. |
| Correctness and edge cases | `checked_finding` | Four immediate pin retries can exhaust during normal config churn; bigint identity separates adjacent unsafe values. |
| Scope discipline | `checked_no_finding` | Local single-PC MVP and P4 boundary preserved. |
| Changed files/direct dependencies | `checked_finding` | All nine changed paths and direct config pin/upload cleanup/process-output dependencies inspected. |
| API/data/config/workflow compatibility | `checked_finding` | Exact-head Ubuntu 21/23 and Windows 23/23. |
| Error handling/diagnostics | `checked_finding` | Restart fails closed after bounded retries, but without allowing normal transient config settling. |
| Security/secret handling | `checked_no_finding` for exact-identity product path | Bigint identity avoids demonstrated Windows Number collision; legacy unsafe state is not used as deletion authority. |
| Tests/validation adequacy | `checked_finding` | 101-page fixture is substantive; DR-003 digest uses mutable pathname and A/B identity assertions use lossy numeric stats. |
| Current-HEAD CI | `checked_finding` | Run 36045163545 Ubuntu 21/23 failed; Windows 23/23 passed. |
| Reports/tracking/documentation | `checked_finding` | README legacy-upload mismatch wording exceeds implementation; prior stale report status corrected. |
| Regression/maintainability | `checked_finding` | Config churn retry and identity-precision fixture require targeted correction. |

`document-wording-review` was reapplied to README, independent-fixes/regression reports, task statuses and this report, separately from passing mechanical Markdown/term lint. Meaning `checked_finding` for the README legacy-manifest recovery branch; identification, approved usage and readability `checked_no_finding`. No terminology approval changed and no policy conflict was found. Wording result: `fail` pending precise recovery wording.

**Bounded normal fix-verification verdict: `fail` on `0696dee9261b72875d3797d238464fe6b595f057`.** IFR-004 / Medium is closed by the drain-aware watcher and actual focused/Ubuntu evidence, and the previously closed five IFR items retain their identities and severities. IFR-002 / High remains open on the Ubuntu restart CI failure; the DR-003 fixture and README direct effects also require correction. Recheck only these bounded items and matching two-OS CI on the next immutable head. No product fix, commit, push or merge was performed by this reviewer. `report_attestation_allowed: false`.

## Bounded fix verification on 00e6939

Mode: same normal reviewer, limited to IFR-002 / High and the four finite direct actions left by the `0696dee` review. Immutable source target `00e693923f7ce8709386b089707fb9eefda32d76`, base `0696dee9261b72875d3797d238464fe6b595f057`. I inspected all nine changed paths and the directly affected config-pin, pruning, transfer-identity, and fixture dependencies. The other six MVP-IFR findings retain their original identities, severities and previous closure decisions; this is not a new broad review.

| Finding / original severity | Production and fixture verification | Disposition |
| --- | --- | --- |
| RDMCP-MVP-IFR-002 / High | `src/index.ts:258-306` keeps pin capture under the config mutex, retains each verified link-first inode, prunes only retired sole-link pins before checking the 64-record cap, and retries a changing config with 10–100 ms backoff for at most 20 attempts/about two seconds. A missing config after creation now fails or retries; only initial precreation may be missing. The real-link fixture replaces config after five successful links, then settles and requires A/B aliases denied and ordinary read allowed; an every-link replacement must fail within the budget without a ready Commander generation/audit. The 70-version live fixture derives its known alias from an actual retained pin and checks exact bigint identity. | Product retry and direct behavior passed the matching Ubuntu and Windows jobs, but the Windows bigint-identity fixture failed by assuming a retained pin must exist after startup pruning. Overall gate remains `open` pending a corrected fixture and passing two-OS exact-head CI. Original High severity retained. |
| DR-003 direct digest/identity fixtures | The swapped upload-temp case hashes a retained hard link to the exact target inode before and after the rejected write, rather than the mutable `config.json` pathname. A/B, historical and race alias-to-pin identity assertions now use bigint stats. | Focused DR003 4/4 pass and Ubuntu CI 4/4; Windows bigint-identity fixture fails before alias creation when the pin directory is empty. Its lifecycle assumption needs correction, without weakening rejection/identity assertions. This does not reopen the original protected-inode product finding on the present evidence. |
| README recovery wording | The new paragraph distinguishes safe but unmatched legacy upload records, which are dropped while the user-root file is preserved, from invalid/unsafe records, which refuse startup. A safe unmatched config-pin identity still refuses startup. This matches the reviewed cleanup/migration branches. | Corrected. No new terminology or readability finding. |

Independent actual Node 22.23.3 focused IFR-002 tests exited 0 with 2/2 passed, zero failed/cancelled/skipped/todo, in 21.174 seconds; focused DR003 tests exited 0 with 4/4 in 21.817 seconds. `npm.cmd run check`, `npm.cmd run lint` (42 Markdown files, zero issues), and `git diff --check 0696dee 00e6939` passed. The implementation owner reports the awaited full actual Node 22.23.3 suite passed 24/24 with zero failed/cancelled/skipped in 75.635 seconds, plus build, check and lint. Matching [GitHub Actions run 36046213583](https://github.com/ssaattww/RemoteDesktopMCP/actions/runs/36046213583) passed Ubuntu job `107790302517` with 24/24 and failed Windows job `107790302868` with 23/24. The sole Windows failure at `test/regressions.test.ts:291` passed `undefined` to `path.join` because `(await readdir(pinDirectory))[0]` was absent. `initialize()` explicitly prunes sole-link pins after DC startup; the fixture cannot assume a pin remains before deliberately capturing one. Required action: in every fixture requiring a retained pin, explicitly invoke the serialized identity-capture method, then link the verified retained pin before any other service operation, and retain exact bigint alias/rejection assertions. The CI failure is a fixture lifecycle assumption; it does not establish product access to the protected inode.

| Required criterion | Disposition | Evidence |
| --- | --- | --- |
| Requirement/design conformance | `checked_no_finding` for production change | Bounded retry and retained known history pass local and exact-head Ubuntu; the Windows fixture fails before its alias assertion. |
| Correctness and edge cases | `checked_no_finding` for production change | Five real replacements settle; endless replacements fail closed and do not publish ready state; production paths pass both OS jobs. |
| Scope discipline | `checked_no_finding` | No change to local single-PC MVP or deferred P4 boundary. |
| Changed files/direct dependencies | `checked_no_finding` | Full nine-path diff and pin/prune/identity/fixture paths inspected. |
| API/data/config/workflow compatibility | `checked_finding` | No public API or manifest-format change in this delta, but exact-head Windows suite is 23/24. |
| Error handling/diagnostics | `checked_no_finding` | Transient link errors are bounded and sustained churn remains a visible initialization failure without ready audit. |
| Security/secret handling | `checked_no_finding` | Verified pins remain retained; unknown state is not used to authorize deletion. |
| Tests/validation adequacy | `checked_finding` | Real-link five-change and endless-change fixtures and exact digest pass; the bigint-identity fixture assumes a post-prune pin exists at `test/regressions.test.ts:291`. |
| Current-HEAD CI | `checked_finding` | Run 36046213583 Ubuntu 24/24 passed, Windows 23/24 failed on the pin-directory fixture assumption. |
| Reports/tracking/documentation | `checked_no_finding` | Matrix and progress text attribute local evidence to this target and keep CI/review pending; README distinguishes migration branches. |
| Regression/maintainability | `checked_finding` | Correct the pin-capture fixture assumption before two-OS closure; bounded retry itself has passing direct CI coverage. |

`document-wording-review` was reapplied to changed README, repair/regression reports, task statuses and this passage separately from mechanical lint. Technical meaning, evidence attribution, approved usage and readability are `checked_no_finding` after the legacy-manifest wording correction. No terminology approval changed. Wording result: `pass`.

**Bounded verdict: `fail` on `00e693923f7ce8709386b089707fb9eefda32d76`.** IFR-002 / High remains open because the exact-head Windows suite failed on the pin-directory fixture assumption, despite direct product behavior passing in both OS jobs. The other six MVP-IFR findings remain closed at their original severities; DR-003 product closure is not reopened by this fixture failure. Recheck only the explicit pin-capture fixture and matching two-OS CI on the next immutable head. No product fix, commit, push or merge was performed by this reviewer. `report_attestation_allowed: false`.

## Bounded fixture closure on 2d5be22

Mode: same normal reviewer; immutable target `2d5be223ea52af35a8c0f29c7ace74362ff9b1cd` against `00e693923f7ce8709386b089707fb9eefda32d76`. Scope is the Windows CI pin-directory fixture assumption and direct report wording only. Product, package and workflow bytes are unchanged. The other six MVP-IFR findings retain their original identities, severities and closed dispositions; IFR-002 / High retains its ID and severity and is closed by this matching two-OS gate.

`test/fixture.ts` now calls serialized `rememberProtectedConfigIdentity()` before selecting a private pin, checks the pin's bigint device/inode key against the protected identity map, and, when an alias is requested, links and verifies that exact inode immediately. The six formerly bare first-entry selections in `test/independent-fixes.test.ts` and `test/regressions.test.ts` use this helper. Startup may legitimately prune every sole-link pin after Commander config updates, so the helper establishes the fixture precondition instead of assuming a post-start pin directory is nonempty. The existing alias rejection, 70-version, five-settle/endless-churn, exact identity, unsafe-manifest and swapped-temp assertions remain. Reports and task status accurately retain previous CI failure and distinguish the new local result from pending CI.

Independent actual Node 22.23.3 focused IFR-002 cases passed 2/2, zero failed/cancelled/skipped/todo, in 21.178 seconds; focused DR003 cases passed 4/4 in 21.786 seconds. `npm.cmd run lint` passed with 42 Markdown files and zero issues; `git diff --check 00e6939 2d5be22` passed. The test owner reports a frozen full actual Node 22.23.3 suite of 24/24 passed, zero failed/cancelled/skipped, in 79.391 seconds. Matching [GitHub Actions run 36047156609](https://github.com/ssaattww/RemoteDesktopMCP/actions/runs/36047156609) passed Ubuntu job `107793413685` and Windows job `107793413829`, each 24/24 with zero failed/cancelled/skipped. The prior candidate's Windows failure remains historical and is not used as evidence for this head.

| Required criterion | Disposition | Evidence |
| --- | --- | --- |
| Requirement/design conformance | `checked_no_finding` | Fixture captures a verified protected pin before testing aliases; product behavior unchanged and both OS jobs pass. |
| Correctness and edge cases | `checked_no_finding` | Empty post-prune directory is handled by explicit capture; alias identity is checked with bigint values. |
| Scope discipline | `checked_no_finding` | Seven-path fixture/report delta only; local MVP/P4 boundary unchanged. |
| Changed files/direct dependencies | `checked_no_finding` | Full delta and serialized capture/map-backed pin dependency inspected. |
| API/data/config/workflow compatibility | `checked_no_finding` | No product/API/config/workflow delta; exact-head two-OS tests pass. |
| Error handling/diagnostics | `checked_no_finding` | Missing verified pin fails an explicit fixture assertion rather than creating an alias from an undefined path. |
| Security/secret handling | `checked_no_finding` | Exact protected map key and alias inode are asserted; protection assertions remain intact. |
| Tests/validation adequacy | `checked_no_finding` | All six former first-entry selections corrected; focused 6/6, full local 24/24, Ubuntu 24/24 and Windows 24/24 pass. |
| Current-HEAD CI | `checked_no_finding` | Run 36047156609, Ubuntu job 107793413685 and Windows job 107793413829 both successful, 24/24 each. |
| Reports/tracking/documentation | `checked_no_finding` | Changed reports preserve candidate-specific evidence and do not prematurely claim closure. |
| Regression/maintainability | `checked_no_finding` | Shared capture helper removes repeated lifecycle assumption; both OS suites pass. |

`document-wording-review` was reapplied to the repair/regression reports, task status and this passage separately from mechanical lint. Technical meaning, retained IDs/severities, candidate attribution, approved usage and readability are `checked_no_finding`; wording result: `pass`.

**Bounded verdict: `pass` on `2d5be223ea52af35a8c0f29c7ace74362ff9b1cd`.** IFR-002 / High is closed; all seven MVP-IFR findings now retain their original identities and severities with closed dispositions. The direct DR-003 fixture correction passes both OS jobs without reopening that earlier product finding. This is a bounded normal-review closure, not the separately reserved independent attestation. No product fix, commit, push or merge was performed by this reviewer. `report_attestation_allowed: true` for this normal-review gate.
